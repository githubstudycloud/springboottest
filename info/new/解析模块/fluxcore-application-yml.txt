server:
  port: 8080
  servlet:
    context-path: /fluxcore

spring:
  application:
    name: platform-fluxcore
  config:
    import:
      - classpath:datasource.yml
  profiles:
    active: dev
  
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations:
    - classpath:mapper/**/*.xml

logging:
  level:
    com.platform.fluxcore: debug
    org.springframework: info
