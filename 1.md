是的，在MySQL数据库中，主从同步确实可能导致锁表问题，特别是在您描述的复杂架构中（1个公共库、6个业务库和50多个应用程序）。

主从同步导致锁表的常见原因包括：

1. 从库重放大事务：当主库执行了大型事务，从库需要重放这些事务时，可能会长时间持有锁

2. 主库写入压力大：如果主库写入操作频繁，而从库处理能力有限，会导致从库落后，进而引起锁等待

3. 网络延迟：如果主从之间网络状况不佳，会导致同步延迟增加

4. 表结构不合理：例如缺少合适的索引，或者没有设置合理的主键

5. 从库资源不足：虽然您提到CPU和磁盘有50%空闲，但可能是内存或网络带宽成为了瓶颈

针对主从同步导致的锁表问题，可以考虑以下解决方案：

1. 检查锁情况：使用`SHOW PROCESSLIST`和`SHOW ENGINE INNODB STATUS`命令查看当前锁等待情况

2. 优化事务大小：将大事务拆分为小事务，减少锁持有时间

3. 调整同步配置：比如修改`slave_parallel_workers`参数增加从库并行复制线程数

4. 检查网络连接：确保主从之间网络连接良好

5. 优化表结构：添加必要的索引，特别是被频繁查询的字段

6. 考虑读写分离：将读操作引导到从库，减轻主库压力

您可能需要查看binlog和relay log来确定具体是哪些表或查询导致了同步延迟和锁表问题。
