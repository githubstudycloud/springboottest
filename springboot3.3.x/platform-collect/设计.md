version1 https://claude.ai/chat/15b4ded8-ffeb-4ead-b2bc-c0e8ea9f55b7
基于您的需求，我重新设计系统架构和目录结构：

```
business-testcase/
    src/
        main/
            java/
                com/
                    study/
                        collect/
                            business/
                                testcase/
                                    config/
                                        MongoConfig.java                # MongoDB配置
                                        ObjectPoolConfig.java          # 对象池配置
                                        ThreadPoolConfig.java          # 线程池配置（新增队列管理）
                                    constant/
                                        CollectionConstants.java       # 集合相关常量
                                        VersionType.java              # 版本类型枚举
                                    controller/
                                        UriCollectController.java     # 异步请求处理
                                    entity/
                                        BaseEntity.java               # 基础实体
                                        VersionEntity.java            # 版本实体
                                        UriEntity.java               # URI实体（优化索引）
                                    manager/
                                        CollectTaskManager.java       # 采集任务管理（新增）
                                        QueueManager.java             # 队列管理（新增）
                                    model/
                                        param/
                                            CollectParam.java         # 采集参数（增加serverUri）
                                            DeleteParam.java          # 删除参数（新增）
                                            QueryParam.java          # 查询参数（新增）
                                            PageParam.java           # 分页参数
                                        response/
                                            AsyncResponse.java        # 异步响应（新增）
                                            TaskResponse.java         # 任务响应（新增）
                                            PageResponse.java
                                            VersionResponse.java
                                    repository/
                                        UriRepository.java           # 优化动态表名处理
                                    service/
                                        UriCollectService.java       # 服务接口
                                        http/
                                            UriHttpService.java      # HTTP服务（限流）
                                        impl/
                                            UriCollectServiceImpl.java # 实现类
                                    utils/
                                        StreamProcessManager.java     # 流式处理（整合清理）
                                        RateLimiter.java             # 限流器（新增）
                                        HttpUtil.java                # HTTP工具类
                                        HashUtil.java                # Hash工具类
```

主要流程设计：

1. 请求处理流程：
    - Controller接收请求后立即返回taskId
    - 任务进入QueueManager排队
    - CollectTaskManager管理任务执行
    - 支持任务优先级调整

2. 采集流程控制：
    - HTTP请求限流（500/min）
    - MongoDB写入限流
    - 批量处理优化
    - 多任务并发控制

3. 数据处理优化：
    - 动态表名处理
    - 批量操作优化
    - 索引优化
    - 对象池优化

4. 清理流程优化：
    - 批量删除优化
    - 并发控制
    - 动态表名支持
    - 软删除/硬删除支持

5. 查询优化：
    - 动态表名查询
    - 批量查询优化
    - 删除标记过滤
    - URI前缀匹配

6. 监控和管理：
    - 任务状态查询
    - 队列状态查询
    - 执行进度查询
    - 优先级调整

这样的架构设计能满足：

1. 请求异步处理
2. 并发控制
3. 动态配置
4. 性能优化
5. 可观测性

是否需要调整这个设计方案？如果确认，我们可以开始逐个实现这些组件。